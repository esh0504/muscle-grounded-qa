# Config / training
hydra-core>=1.3.0
omegaconf>=2.3.0

# Vision / media
opencv-python>=4.8.0
Pillow>=10.0.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9
av>=12.0.0

# Hugging Face
huggingface_hub>=0.25.0
transformers>=4.44.0
datasets>=2.21.0
accelerate>=0.33.0
peft>=0.11.0
safetensors>=0.4.0
tokenizers>=0.19.0

# Mesh (SpiralNet++ spiral extraction; no cp311 wheel — the Dockerfile builds
# it from source with setuptools<70. Bare metal: pip install "setuptools<70"
# first, then openmesh.)
# openmesh==1.2.1   (installed in its own Docker layer, see Dockerfile)

# Scientific / common utils
numpy>=1.26.0
scipy>=1.11.0
pandas>=2.1.0
tqdm>=4.66.0
matplotlib>=3.8.0
scikit-learn>=1.3.0
einops>=0.7.0
timm>=1.0.0

# Logging
wandb>=0.17.0


# Headless 3D mesh viz (pyrender optional; matplotlib Agg always works)
trimesh>=4.0.0
pyrender>=0.1.45
PyOpenGL>=3.1.0
PyOpenGL-accelerate>=3.1.0; platform_system != "Darwin"
PyVirtualDisplay>=3.0
